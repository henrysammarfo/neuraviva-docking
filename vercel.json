```json
{
    "version": 2,
    "functions": {
        "api/index.ts": {
            "includeFiles": "{server,shared}/**"
        }
    },
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api"
        },
        {
            "src": "/((?!api/).*)",
            "dest": "/api"
        }
    ]
}